# Pinned to the versions verified working with Python 3.13 (see .venv)
customtkinter==5.2.2
pandas==2.2.3
numpy==2.2.4
matplotlib==3.10.1
yfinance==1.5.2
backtesting==0.6.4
scikit-learn==1.6.1
joblib==1.4.2
python-dotenv==1.1.0
TA-Lib==0.6.3            # Requires the C library first -- see TALIB_INSTALLATION.md (Windows: pre-built wheel)
ephem==4.2               # Astronomical calculations (Real Moon strategy)
anthropic==0.89.0        # Claude API (sentiment + AI trade decisions)
alpaca-trade-api==3.2.0  # Alpaca broker SDK -- being replaced by alpaca-py (see plan Phase 5)
requests==2.32.3         # Discord webhooks
pytz==2025.2             # Market-hours timezone handling -- removed in Phase 6
